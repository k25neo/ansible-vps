[defaults]
inventory = hosts
roles_path = roles
remote_user = root
host_key_checking = False
retry_files_enabled = False

[privilege_escalation]
become = True
become_method = sudo

[ssh_connection]
pipelining = True
